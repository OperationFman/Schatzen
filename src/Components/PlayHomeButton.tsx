import * as React from "react";
import Button from "@mui/material/Button";

export const PlayHomeButton = () => {
  return (
    <Button variant="contained" color="info">
      PLAY
    </Button>
  );
};
